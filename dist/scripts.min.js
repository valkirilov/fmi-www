var Generator = function() {
    var a = {
        image: {
            width: null,
            height: null,
            category: null,
            result: null
        },
        text: {
            paragraphs: null,
            len: null,
            result: null
        }
    }, b = function() {
        c(), d();
    }, c = function() {
        a.image.width = $("#generator-image-width"), a.image.height = $("#generator-image-height"), 
        a.image.category = $("#generator-image-category"), a.image.result = $("#generator-image-result"), 
        a.text.paragraphs = $("#generator-text-paragraphs"), a.text.len = $(".generator-text-length"), 
        a.text.result = $("#generator-text-result");
    }, d = function() {
        a.image.width.on("change", function() {
            e();
        }), a.image.height.on("change", function() {
            e();
        }), a.image.category.on("change", function() {
            e();
        }), a.text.paragraphs.on("change", function() {
            f();
        }), a.text.len.on("change", function() {
            f();
        });
    }, e = function() {
        var b = "";
        "" !== a.image.width.val() && (b += (b.length > 0 ? "&" : "") + "w=" + a.image.width.val()), 
        "" !== a.image.height.val() && (b += (b.length > 0 ? "&" : "") + "h=" + a.image.height.val()), 
        "" !== a.image.category.val() && (b += (b.length > 0 ? "&" : "") + "c=" + a.image.category.val()), 
        a.image.result.val("/i.php?" + b);
    }, f = function() {
        var b = "";
        "" !== a.text.paragraphs.val() && (b += (b.length > 0 ? "&" : "") + "p=" + a.text.paragraphs.val()), 
        "" !== a.text.len.val() && (b += (b.length > 0 ? "&" : "") + "l=" + a.text.len.val()), 
        a.text.result.val("/t.php?" + b);
    };
    return {
        init: b
    };
}();

$(document).ready(function() {
    Generator.init();
});